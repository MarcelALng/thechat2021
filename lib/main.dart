import 'package:flutter/material.dart';

import 'constants/routes_constant.dart';

void main() => TheChatApp();

class TheChatApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      initialRoute: ,
      routes:  ,
    );
  }
}
